## 파일 구조

```
Sources/docker/
├── Dockerfile.baseImage    # F Prime 및 SGFuzz 기본 환경 구축
├── Dockerfile.fsgfuzz      # 퍼징 작업 실행 환경
├── docker-compose.yml      # Docker Compose 설정
└── README.md              # 이 파일
```
